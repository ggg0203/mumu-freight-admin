# 幕幕货运管理系统 - 代码实现深度解析

**重点：亮点功能的核心代码实现、关键代码位置、为什么这么写**

---

## 1. WebSocket 实时推送系统

### 核心文件
| 文件 | 作用 |
|------|------|
| `src/services/websocket.ts` | WebSocket 服务单例（核心） |
| `src/hooks/useRealtime.ts` | React Hook 封装 |
| `src/components/RealtimeIndicator/index.tsx` | LIVE 指示灯组件 |

### 关键代码详解

#### websocket.ts - Observer 模式实现
```typescript
// 为什么用 Map<EventType, Set<Callback>>？
// 因为 Set 保证同一回调不会重复注册，Map 按事件类型分组便于派发
private listeners = new Map<RealtimeEventType, Set<EventCallback>>();

// 订阅方法返回 unsubscribe 函数
// 为什么返回函数？方便 useEffect 的 cleanup 直接调用
on(type: RealtimeEventType, cb: EventCallback): () => void {
  if (!this.listeners.has(type)) {
    this.listeners.set(type, new Set());
  }
  this.listeners.get(type)!.add(cb);
  return () => this.listeners.get(type)?.delete(cb);
}

// 派发事件 - 遍历该类型的所有回调
private dispatch(type: RealtimeEventType, data: any): void {
  const cbs = this.listeners.get(type);
  if (cbs) {
    cbs.forEach(cb => {
      try { cb(event); } catch (e) { console.error('[WS] callback error:', e); }
      // 为什么 try-catch？防止一个组件的错误阻塞其他组件
    });
  }
}
```

#### websocket.ts - Mock 引擎
```typescript
// 40% 创建新订单 / 40% 更新状态 / 20% 变更司机
private mockTick(): void {
  const rand = Math.random();
  if (rand < 0.4) this.mockOrderCreated();
  else if (rand < 0.8) this.mockOrderUpdated();
  else this.mockDriverStatusChanged();
}

// 创建新订单时：
// 1. 生成 OrderInfo 对象
// 2. 调用 setOrders() 持久化到 shared-data
// 3. dispatch('order.created', newOrder)
// 4. 调用 NotificationStore.addNotification()
//
// 为什么按这个顺序？
// 因为 Dashboard 和 Order 页面都订阅了 order.created 事件，
// 收到事件后从 shared-data 重新读取数据，所以必须先持久化
```

#### useRealtime.ts - React Hook
```typescript
// 为什么用 optionsRef 存储 options？
// useEffect 中订阅事件后，options.onEvent 如果变化，
// 需要重新订阅才能拿到最新的回调
const optionsRef = useRef(options);
optionsRef.current = options;

useEffect(() => {
  const unsub = wsService.onAny((event) => {
    setLatestEvent(event);
    setEventLog(prev => [event, ...prev].slice(0, 20));
    // 限制 20 条，防止内存泄漏
    optionsRef.current?.onEvent?.(event);
  });
  return () => unsub();
}, []);
```

---

## 2. 性能监控面板

### 核心文件
| 文件 | 作用 |
|------|------|
| `src/hooks/usePerfMonitor.ts` | 性能数据采集（核心） |
| `src/components/PerfPanel/index.tsx` | 悬浮面板 UI |
| `src/components/PerfPanel/index.module.css` | 面板样式 |

### 关键代码详解

#### usePerfMonitor.ts - FPS 采集
```typescript
// 为什么用 requestAnimationFrame 而非 setInterval？
// rAF 在页面不可见时自动暂停，不浪费资源
// 保留最近 60 帧，计算 1 秒内的帧数
const tick = (timestamp: number) => {
  frameTimesRef.current.push(timestamp);
  if (frameTimesRef.current.length > 60) {
    frameTimesRef.current = frameTimesRef.current.slice(-60);
  }
  rafIdRef.current = requestAnimationFrame(tick);
};
rafIdRef.current = requestAnimationFrame(tick);

// 每秒计算一次 FPS
const fps = (times.length - 1) / (elapsed / 1000);
```

#### usePerfMonitor.ts - fetch 拦截（核心难点）
```typescript
// ❌ 错误写法 - 导致 Illegal invocation
originalFetchRef.current = window.fetch;
return originalFetchRef.current!(input, init);
// 原因：浏览器原生 fetch 需要 this === window

// ✅ 正确写法
return originalFetchRef.current!.call(window, input, init);
// .call(window) 显式绑定 this 上下文

// 完整拦截逻辑
const patchedFetch = (input, init) => {
  const start = performance.now();
  const url = typeof input === 'string' ? input : input.url;
  return originalFetchRef.current!.call(window, input, init)
    .then(resp => {
      // 成功：记录耗时和状态码
      apiTimingsRef.current.unshift({ url, duration, status: resp.status, time: Date.now() });
      return resp;
    })
    .catch(err => {
      // 失败：状态码 0
      apiTimingsRef.current.unshift({ url, duration, status: 0, time: Date.now() });
      throw err; // 重新抛出，不影响原调用方
    });
};
```

---

## 3. PWA 离线安装

### 核心文件
| 文件 | 作用 |
|------|------|
| `vite.config.ts` | VitePWA 插件配置 |
| `src/components/PWAInstallPrompt/index.tsx` | 安装提示组件 |
| `public/icons/icon-192x192.svg` | 应用图标 |

### 关键配置详解

#### vite.config.ts
```typescript
VitePWA({
  registerType: 'autoUpdate',  // 自动更新 SW
  manifest: {
    name: '幕幕货运管理系统',
    display: 'standalone',      // 独立窗口
    theme_color: '#1677ff',     // 主题色
  },
  workbox: {
    globPatterns: ['**/*.{js,css,html,svg,png,ico,json}'],
    // 排除地图 CDN，避免 SW 拦截
    navigateFallbackDenylist: [/^\/mapapi/, /map\.qq\.com/],
  },
})
```

#### PWAInstallPrompt/index.tsx
```typescript
// 为什么用 beforeinstallprompt 事件？
// 浏览器标准事件，Chrome/Edge/Safari 都支持
// 返回 BeforeInstallPromptEvent 包含 prompt() 方法
window.addEventListener('beforeinstallprompt', handler);

// 为什么存 deferredPrompt？
// beforeinstallprompt 只能触发一次，必须存储引用
// 用户点击"安装"按钮时调用 deferredPrompt.prompt()
const handleInstall = async () => {
  deferredPrompt.prompt();
  const result = await deferredPrompt.userChoice;
};

// App.tsx - 为什么在 dev 模式注销 SW？
// npm run preview 注册的 SW 在切回 npm run dev 后仍然存在
// 可能拦截地图 SDK 加载请求
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.getRegistrations().then(r => {
    r.forEach(reg => reg.unregister());
  });
}
```

---

## 4. 运单轨迹追踪优化

### 核心文件
| 文件 | 作用 |
|------|------|
| `src/pages/OrderTrace/index.tsx` | 运单追踪页面（核心） |
| `src/utils/drivingRouteUtils.ts` | 驾车路线 API 工具 |
| `src/components/MapContainer/index.tsx` | 地图容器 |

### 关键代码详解

#### OrderTrace - 当前位置插值
```typescript
// 为什么自己插值而非用 currentOrder.currentLocation？
// currentLocation 基于模拟轨迹索引计算，
// 当 displayTracks 切换为真实路线时，蓝点位置不匹配
const interpolatedCurrentLoc = useMemo(() => {
  if (displayTracks.length >= 2 && currentOrder.progress > 0) {
    const progress = currentOrder.progress / 100;
    const segFloat = progress * (displayTracks.length - 1);
    const segIdx = Math.min(Math.floor(segFloat), displayTracks.length - 2);
    const segRatio = segFloat - segIdx;
    const from = displayTracks[segIdx];
    const to = displayTracks[segIdx + 1];
    // 线性插值：from + (to - from) * ratio
    return {
      lat: from.lat + (to.lat - from.lat) * segRatio,
      lng: from.lng + (to.lng - from.lng) * segRatio,
    };
  }
}, [displayTracks, currentOrder.progress]);
```

#### OrderTrace - API 失败处理
```typescript
// 新增 apiFailed state
const [apiFailed, setApiFailed] = useState(false);

// displayTracks 不再回退到模拟轨迹
const displayTracks = useMemo(() => {
  if (realRouteTracks.length > 0) return realRouteTracks;
  // pending/delivered 显示模拟轨迹（计划路线/完成路线）
  if (status === 'pending' || status === 'delivered') return order.track;
  return [];  // API 失败或加载中 → 不显示轨迹
}, [realRouteTracks, status]);

// 失败提示条 + 重试按钮
{!routeLoading && apiFailed && (
  <Alert message="无法加载驾车路线数据"
    type="warning"
    action={<Button size="small" onClick={retry}>重试</Button>}
  />
)}
```

#### drivingRouteUtils.ts - 真实道路 API
```typescript
// decodePolyline：腾讯地图差分解码
// 为什么这样解码？
// API 返回的 polyline 是扁平差值数组：
// [startLat, startLng, diffLat1, diffLng1, diffLat2, diffLng2, ...]
// 每个差值除以 1,000,000 后累加得到真实坐标
const decodePolyline = (raw: number[]) => {
  let lat = raw[0], lng = raw[1];
  for (let i = 2; i < raw.length - 1; i += 2) {
    lat += raw[i] / 1000000;
    lng += raw[i + 1] / 1000000;
    pts.push({ lat, lng });
  }
};

// 生产/开发模式自动切换 URL
const API_BASE = import.meta.env.DEV
  ? '/mapapi/?'          // 开发：Vite 代理
  : 'https://apis.map.qq.com/ws/direction/v1/driving/?'; // 生产：直连
```

#### MapContainer - 防地图重绘
```typescript
// 为什么用 ref 存 initialCenter？
// 父组件每次 re-render 创建新对象 → {} !== {} → useEffect 重新执行
// → destroy() + new Map() → 地图闪烁
const stableCenterRef = useRef(initialCenter);
const stableZoomRef = useRef(initialZoom);

// 为什么 initMap 用空依赖 []？
// 只希望初始化一次，不追踪任何 props
const initMap = useCallback(() => {
  const center = stableCenterRef.current;  // 从 ref 读取
  const map = new TMap.Map(container, {
    center: new TMap.LatLng(center.lat, center.lng),
    zoom: stableZoomRef.current,
  });
  map.on('click', (evt) => onMapClickRef.current?.(...));
  mapRef.current = map;
}, []);

// 回调防 stale：用 ref 存最新回调
const onMapClickRef = useRef(onMapClick);
onMapClickRef.current = onMapClick;  // 每次渲染更新
// initMap 闭包中读取 onMapClickRef.current（最新回调）
```

---

## 5. 所有亮点文件索引

| 功能 | 核心文件 |
|------|---------|
| WebSocket 实时推送 | `src/services/websocket.ts` |
| useRealtime Hook | `src/hooks/useRealtime.ts` |
| LIVE 指示灯 | `src/components/RealtimeIndicator/index.tsx` |
| 性能监控 Hook | `src/hooks/usePerfMonitor.ts` |
| 性能面板 UI | `src/components/PerfPanel/index.tsx` |
| PWA 配置 | `vite.config.ts` |
| 安装提示组件 | `src/components/PWAInstallPrompt/index.tsx` |
| 应用图标 | `public/icons/icon-192x192.svg` |
| 运单追踪 | `src/pages/OrderTrace/index.tsx` |
| 驾车路线 API | `src/utils/drivingRouteUtils.ts` |
| 地图容器 | `src/components/MapContainer/index.tsx` |
| AI 服务 | `src/services/ai.ts` |
| AI 异常检测 | `src/pages/OrderTrace/aiAnomalyDetection.ts` |
| AI 智能填单 | `src/pages/Order/aiFillOrder.ts` |
| AI 对话分析 | `src/pages/ReportCenter/aiReportChat.ts` |
| AI 数据洞察 | `src/pages/Dashboard/aiInsight.ts` |
| 共享数据层 | `src/shared-data.ts` |
| 通知 Store | `src/stores/notificationStore.ts` |
| Tab 多标签 | `src/stores/tabStore.ts` |
| 数据大屏 | `src/pages/BigScreen/index.tsx` |
| 3D 货运网络 | `src/pages/ThreeGlobe/index.tsx` |
