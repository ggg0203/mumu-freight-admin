# SideMenu

> 侧边栏菜单 — 按权限过滤的动态菜单

## 组件文件

| 文件 | 作用 |
|------|------|
| `index.tsx` | SideMenu：从 userStore 读取菜单列表 → 递归转换为 Ant Design Menu items → 折叠/展开 |

## 样式文件

| 文件 | 作用 |
|------|------|
| `index.module.css` | 侧边栏样式：暗色主题、菜单项悬浮高亮 |

