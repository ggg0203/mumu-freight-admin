# ExcelImport

> Excel 导入组件 — 通用表格导入功能

## 组件文件

| 文件 | 作用 |
|------|------|
| `index.tsx` | ExcelImport：上传 Excel → 解析行列 → 数据映射 → 预览 → 确认导入 |

## 样式文件

| 文件 | 作用 |
|------|------|
| `index.module.css` | 导入弹窗样式 |

