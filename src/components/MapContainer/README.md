# MapContainer

> 腾讯地图容器 — 地图组件封装（核心复用组件）

## 组件文件

| 文件 | 作用 |
|------|------|
| `index.tsx` | MapContainer：TMap 初始化（防重绘）、标注点、轨迹折线、自适应缩放、轨迹回放动画 |

## 样式文件

| 文件 | 作用 |
|------|------|
| `index.module.css` | 地图容器样式：工具栏定位、加载遮罩 |

